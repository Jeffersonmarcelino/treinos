// método MAP 

let lista = ['arroz', 'feijao', 'carne', 'azeite', 'pao']

lista.map((item) => {
       console.log(item)
})

let notas = [ 9, 8, 10, 4.5, 7]

notas.map((todasNotas) => {
    let numero = todasNotas + 1
        console.log(numero)

})

console.log("o tamanho da lista é ")
console.log(lista.length)

